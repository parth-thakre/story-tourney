# Story Tourney Frontend

Next.js frontend for the Story Tourney app.

## Run

```bash
npm install
npm run dev
```

The app expects the backend at `http://localhost:3001` by default through the rewrite in `next.config.ts`.

Main UI surfaces:
- setup
- live tournament run
- results
- history

See the root `README.md` for full project setup.
